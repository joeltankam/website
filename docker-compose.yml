version: '3.8'

services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: joeltankam-blog
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    labels:
      - "com.example.description=Joël Tankam's Blog Website"
      - "com.example.version=1.0.0"
